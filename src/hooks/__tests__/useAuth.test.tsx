import { vi } from 'vitest';
import { render, screen } from '../../utils/test-utils';

describe('<ProjectsPage />', () => {
  it('should render successfully', () => {
  });

//   it('should call the getProjects function on mount', () => {
//     const spy = vi.spyOn(apiRoutes, 'getProjects')
//     expect(spy.getMockName()).toEqual('getProjects')
//     const {baseElement} =     render(<ProjectsPage />);
//     expect(spy).toHaveBeenCalledTimes(1)
//     console.log("🚀 | file: index.test.tsx:33 | it | baseRender", baseElement);
//   });

});

// it should render a list of projects if there are any
// it should render an empty list there aren't any
// it should render a loading state if the data is loading
// it should render an error state if there is an error
